import React from 'react'
import wordpress from "../../assets/icones/wordpress-icon.svg"

export default function WORDPRESSIcon({width}) {
  return (
    <img src={wordpress} alt="wordpress icon" style={{ width: `${width}` }}/>
  )
}
